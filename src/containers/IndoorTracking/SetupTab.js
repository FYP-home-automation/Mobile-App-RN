import React from 'react';
import { View, StyleSheet } from 'react-native';

const SetupTab = () => <View style={styles.container}></View>;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#ff4081',
  },
});

export default SetupTab;
